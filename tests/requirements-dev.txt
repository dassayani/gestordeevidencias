# Dependências só de teste — não entram no executável.
# Instale com: venv\Scripts\python.exe -m pip install -r tests\requirements-dev.txt
robotframework>=7.0
pyflakes>=3.0          # verificação estática usada antes de empacotar

# O pymupdf também é usado pelos testes, para ler o texto do documento gerado,
# mas está em requirements.txt: virou dependência de execução quando a
# pré-visualização passou a renderizar a página do PDF.
